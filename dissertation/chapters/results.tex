%% Chapter 4: Results
\chapter{Results}
\label{ch:results}

Headline values are held-out test metrics reported as mean~$\pm$~standard deviation over five independent seeds with stratified 70/15/15 splits.

%% ─────────────────────────────────────────────────────────────────────────────
\section{The Simulation-to-Reality Gap}
\label{sec:res-main}

Table~\ref{tab:main-results-raw} reports all four classifiers across the four-condition evaluation matrix on raw 208-dimensional features.

\begin{table}[htbp]
\centering
\caption[Classification accuracy across the four evaluation conditions]{Classification accuracy (\%) on raw features across the four evaluation conditions.}
\label{tab:main-results-raw}
\small
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Model} & \textbf{Clean$\to$Clean} & \textbf{Clean$\to$Noisy} & \textbf{Noisy$\to$Noisy} & \textbf{Noisy$\to$Clean} \\
\midrule
CNN  & 94.00 $\pm$ 6.89 & 19.98 $\pm$ 0.33 & \textbf{75.81 $\pm$ 0.62} & \textbf{52.23 $\pm$ 1.94} \\
MLP  & \textbf{98.81 $\pm$ 0.69} & 21.32 $\pm$ 0.34 & 57.25 $\pm$ 0.80 & 47.61 $\pm$ 0.69 \\
RF   & 91.03 $\pm$ 0.30 & \textbf{22.57 $\pm$ 0.18} & 57.85 $\pm$ 0.64 & 29.91 $\pm$ 3.00 \\
SVM  & 83.38 $\pm$ 0.43 & 20.00 $\pm$ 0.00 & 50.90 $\pm$ 0.72 & 51.66 $\pm$ 0.41 \\
\bottomrule
\end{tabular}
\end{table}

Three findings follow. First, every model achieves strong clean-domain accuracy (83--99\%), confirming the forward model produces separable class representations, so the task itself is not the limiting factor. Second, \emph{every} model collapses to approximately chance under Clean$\to$Noisy. The collapse is architecture-independent and feature-representation-independent (Appendix~\ref{app:feature-reduction}), identifying the gap as \emph{distributional} rather than a deficit of model capacity: no classifier can compensate at inference for an unseen training-time shift. This is considerably more extreme than the degradations of up to 40\% reported for image classifiers by \textcite{hendrycks2019}.

Third, noise-aware training recovers a large fraction of the loss. The CNN reaches 75.81\% under Noisy$\to$Noisy, an improvement of 55.8 percentage points (pp) over the same architecture trained on clean data.

Reconstruction shows the same collapse. Figure~\ref{fig:reconstructions} inverts representative single frames back onto the mesh. Clean measurements localise every contact class distinctly, confirming physically coherent fields and genuine class separability in image space. The same frames under the full four-component corruption reconstruct to artefact, with no recoverable contact location, and the quadrant seams throughout are the per-electrode block structure of the dominant error in the image domain. Direct classification retains 75.81\% where reconstruction retains nothing, the practical case for bypassing the inverse problem.

\begin{figure}[htbp]
\centering
\begin{subfigure}{\textwidth}
\centering
\includegraphics[width=\textwidth]{results/dataset_validation/reconstructions/random_reconstructed_class_images_clean.png}
\caption[Clean measurements]{Clean measurements.}
\label{fig:recon-clean}
\end{subfigure}

\vspace{0.3cm}

\begin{subfigure}{\textwidth}
\centering
\includegraphics[width=\textwidth]{results/dataset_validation/reconstructions/random_reconstructed_class_images_noisy.png}
\caption[Under four-component corruption]{The same frames under the full four-component corruption.}
\label{fig:recon-noisy}
\end{subfigure}
\caption[Reconstructions from clean and noisy measurements]{Difference reconstructions of randomly drawn single frames from each class, solved with a one-step linearised Gauss--Newton method under a Laplace smoothness prior (hyperparameter 0.003). Clean frames localise each contact class; the same frames under the four-component noise model yield no recoverable contact location, and the quadrant seams expose the per-electrode block structure of the corruption. The dissertation classifies the boundary voltages directly and never solves this inverse problem; the reconstructions are shown to characterise the measurement corruption, not as a proposed method.}
\label{fig:reconstructions}
\end{figure}

The CNN outperforms the strongest baseline on noisy data by 17.9\,pp (RF, 57.85\%). The natural explanation is that convolutional kernels exploit the block-structured, per-electrode form of the dominant corruption (Section~\ref{sec:res-ablation}), and this is directly testable. Applying a fixed permutation to the 208 measurement indices, identically to training and test data and \emph{after} noise injection, preserves information content exactly while destroying adjacency. The CNN then loses 30.2\,pp, whereas the three exchangeable baselines are unaffected, the \gls{svm} to the decimal place (Table~\ref{tab:permutation}). The permuted CNN falls below both \gls{rf} and \gls{mlp}, so without block structure the convolutional prior is not merely uninformative but actively harmful.

\begin{table}[htbp]
\centering
\caption[Permutation ablation]{Accuracy (\%) on noisy evaluation before and after a fixed permutation of the measurement vector, applied after noise injection. CNN values are means over 5 seeds; baselines are deterministic given the split.}
\label{tab:permutation}
\small
\setlength{\tabcolsep}{8pt}
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Model} & \textbf{Original} & \textbf{Permuted} & \textbf{Change} \\
\midrule
CNN & \textbf{76.21 $\pm$ 0.52} & \textbf{45.97 $\pm$ 1.37} & \textbf{$-$30.24} \\
RF  & 60.03 & 59.12 & $-$0.91 \\
MLP & 56.96 & 56.99 & $+$0.03 \\
SVM & 52.53 & 52.53 & 0.00 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Training Regimes}
\label{sec:res-regimes}

\begin{table}[htbp]
\centering
\caption[CNN accuracy on noisy evaluation by training regime]{CNN accuracy (\%) on noisy evaluation by training regime, raw features.}
\label{tab:augmented-results}
\small
\begin{tabular}{@{}lc@{}}
\toprule
\textbf{Training Regime} & \textbf{Accuracy} \\
\midrule
Fixed noisy dataset            & \textbf{75.81 $\pm$ 0.62} \\
Online augmentation            & 69.50 $\pm$ 0.62 \\
Mixed (30\% clean + augmented) & 69.17 $\pm$ 0.68 \\
\bottomrule
\end{tabular}
\end{table}

Training on the fixed, pre-generated noisy dataset gives the highest noisy-domain accuracy (75.81\%), exceeding online augmentation by 6.3\,pp, an order of magnitude larger than the 0.62\,pp seed-to-seed spread of either condition. That advantage is specific to the training severity, however, and reverses at higher severities (Section~\ref{sec:res-robustness}).

Both fixed-noise and online-augmented training nonetheless lose accuracy on uncorrupted data (52.2\% and 40.2\%), since neither observes an uncorrupted measurement, the no-contact class especially seen only in perturbed form. Mixed training, retaining 30\% clean samples per batch, recovers this to 71.50\% at a cost of 0.3\,pp on noisy data.

%% ─────────────────────────────────────────────────────────────────────────────
\section{Noise Component Ablation}
\label{sec:res-ablation}

The ablation asks which noise components a training-time model must include, which requires care in how each configuration is evaluated. A model trained with only component $X$ can be tested either on data corrupted by $X$ alone (\emph{matched}), or on the full four-component corruption (\emph{deployment}). Only the latter is operationally meaningful, since real hardware exhibits all error sources simultaneously, so a model omitting a component must nonetheless face it in the field.

\begin{table}[htbp]
\centering
\caption[Single-component noise ablation]{Single-component ablation. Each CNN is trained with one noise component active and evaluated twice: on the same single component (matched) and on the full four-component corruption (deployment).}
\label{tab:ablation-single}
\small
\setlength{\tabcolsep}{8pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Training noise} & \textbf{Matched (\%)} & \textbf{Deployment (\%)} \\
\midrule
Gaussian only           & 96.61 $\pm$ 0.92 & 20.88 $\pm$ 1.34 \\
Contact impedance only  & 90.11 $\pm$ 3.85 & 19.72 $\pm$ 0.20 \\
Electrode bias only     & 78.68 $\pm$ 0.85 & \textbf{57.79 $\pm$ 0.58} \\
Quantisation only       & 92.89 $\pm$ 3.53 & 14.03 $\pm$ 5.50 \\
\midrule
Full four-component     & \multicolumn{2}{c}{75.81 $\pm$ 0.62} \\
\bottomrule
\end{tabular}
\end{table}

The two columns rank the components in opposite orders, and the matched column is misleading. Judged on matched evaluation, electrode bias appears the \emph{most} damaging component (78.68\%, lowest of the four) while Gaussian noise appears benign (96.61\%). Judged on deployment evaluation the conclusion reverses: models trained on Gaussian noise, contact impedance or quantisation alone all perform at or below chance, whereas the electrode-bias model retains 57.79\%. The matched column measures the difficulty of an artificial world containing a single corruption, not the value of modelling it.

Two complementary claims follow. Electrode bias is \emph{necessary}: every configuration omitting it collapses to chance under realistic conditions, regardless of its score in its own matched world. Quantisation-only training is the extreme case, falling below chance (14.03\%) with high seed variance, indicating a model trained on deployment-negligible corruption learns a representation that is not merely uninformative but actively damaging. Electrode bias alone is nonetheless \emph{not sufficient}, falling 18.8\,pp short of the full model, so robust deployment requires the dominant structured error modelled jointly with the remaining sources.

\subsection{Direct Characterisation of the Domain Gap}
\label{sec:res-domain}

The dominance of electrode bias can be confirmed from the measurements themselves, independently of any classifier. Decomposing each vector into signal and noise contributions (Table~\ref{tab:noise-decomposition}) shows electrode bias accounting for effectively all noise energy in every class, exceeding the touch signal by a factor of 4.6 for the largest class and approximately 41 for light touch. The touch signature is a small perturbation on a much larger structured offset; that the offset is block-structured is what keeps the task tractable, and once discounted, the signal-to-residual ratio recovers to 11.3--11.9. Measurement-level decomposition and ablation therefore converge on the same component from independent directions.

\begin{table}[htbp]
\centering
\caption[Signal and noise energy decomposition by class]{Signal and noise energy decomposition by class ($L_2$ norms, means over the dataset). Electrode bias accounts for essentially all noise energy and exceeds the touch signal itself by a factor of 4.6 to 41 depending on class.}
\label{tab:noise-decomposition}
\small
\setlength{\tabcolsep}{6pt}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Class} & \textbf{Signal} & \textbf{Total noise} & \textbf{Bias} & \textbf{Signal:residual} \\
\midrule
No contact          & 0.0000 & 0.1669 & 0.1656 & --- \\
Light touch         & 0.0040 & 0.1655 & 0.1655 & 11.3 \\
Firm press          & 0.0248 & 0.1657 & 0.1657 & 11.5 \\
Point contact       & 0.0058 & 0.1657 & 0.1657 & 11.3 \\
Distributed contact & 0.0359 & 0.1654 & 0.1653 & 11.9 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Cancellation of Electrode Bias Under Differencing}
\label{sec:res-rho}

\textcite{kolehmainen1997} and \textcite{boone1996} report that difference imaging tolerates electrode error constant between the reference and measurement frames, becoming vulnerable only when it changes. Since the model above applies bias directly to $\Delta\mathbf{v}$, sweeping the fraction $\rho$ of bias common to both frames tests this assumption directly (full derivation and results in Appendix~\ref{app:rho}). The response is strongly non-linear: accuracy moves by under 2\,pp across the first half of the range, and even at $\rho = 0.9$ a 14\,pp deficit remains against full cancellation, which requires identical electrode positions and interface state at both acquisitions, the condition \textcite{boone1996} identify as failing in practice through drift. The headline result is therefore insensitive to this assumption everywhere except a physically unrealisable limit.

\subsection{The Minimal Sufficient Noise Model}
\label{sec:res-ablation-minimal}

Enumerating all component subsets identifies how much of the noise model is actually required. Table~\ref{tab:ablation-subset} reports the best subset at each size, ranked by deployment accuracy.

\begin{table}[htbp]
\centering
\caption[Best component subset at each size]{Best component subset at each size, ranked by deployment accuracy.}
\label{tab:ablation-subset}
\small
\setlength{\tabcolsep}{8pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}clcc@{}}
\toprule
\textbf{$n$} & \textbf{Best subset} & \textbf{Deployment (\%)} & \textbf{Matched (\%)} \\
\midrule
1 & Electrode bias                                & 56.85 $\pm$ 1.89 & 77.33 $\pm$ 1.69 \\
2 & Gaussian + electrode bias                     & \textbf{76.53 $\pm$ 0.52} & 76.73 $\pm$ 0.81 \\
3 & Gaussian + electrode bias + quantisation      & 76.90 $\pm$ 0.80 & 77.04 $\pm$ 0.37 \\
4 & All four components                           & \multicolumn{2}{c}{75.81 $\pm$ 0.62} \\
\bottomrule
\end{tabular}
\end{table}

A two-component model of Gaussian noise and electrode bias reaches 76.53\%, indistinguishable from the best three-component subset (76.90\%) and from the full four-component model. Contact impedance and quantisation contribute no measurable deployable robustness once the other two are present.

The claim is scoped to the four-component family: the other two are redundant \emph{given} this noise model, not necessarily sufficient against the fuller error sources of Table~\ref{tab:error-sources}. The necessity result for electrode bias carries no such caveat, since further sources could only strengthen it.

The electrode-bias-only configuration is trained in both enumerations, giving an incidental replication estimate: two independent five-seed runs return 57.79\% and 56.85\%, a 0.94\,pp difference that sets the practical resolution of the ablation and is why the spread across the two-, three- and four-component models is read as agreement rather than an ordering.

Each required component has a distinct role. Electrode bias supplies the dominant structured distortion defining the domain shift (Section~\ref{sec:res-domain}). Gaussian noise supplies the measurement noise floor, which matters disproportionately for the no-contact class: without it, no-contact samples are exact zero vectors in training but carry noise at test time, rendering the class undetectable. The per-component severity sweep isolates this: Gaussian noise at any non-zero severity raises accuracy by 20.1\,pp, corresponding almost exactly to the 20\% of samples in that class (Figure~\ref{fig:severity-sweep}).

That mechanism suggests the two-component result decomposes further: since the no-contact class is an exact zero vector by construction, Gaussian noise's measured value may be confined to rescuing it rather than conferring robustness. Repeating the subset ablation on the four contact classes alone tests this (Table~\ref{tab:no-contact}).

\begin{table}[htbp]
\centering
\caption[Subset ablation excluding the no-contact class]{Deployment accuracy (\%) on the four contact classes only, chance $=25$\%. Means over 5 seeds. Electrode bias alone matches the full model once the degenerate class is removed.}
\label{tab:no-contact}
\small
\setlength{\tabcolsep}{8pt}
\begin{tabular}{@{}lc@{}}
\toprule
\textbf{Training noise} & \textbf{Deployment (\%)} \\
\midrule
Gaussian only        & 25.07 $\pm$ 0.13 \\
Electrode bias only  & 70.53 $\pm$ 0.66 \\
Gaussian + bias      & 71.21 $\pm$ 0.44 \\
Full four-component  & 69.79 $\pm$ 1.50 \\
\bottomrule
\end{tabular}
\end{table}

Electrode bias alone now matches the full model (70.53\% against 69.79\%), adding Gaussian changing it by only 0.68\,pp, within the seed spread, while Gaussian alone sits exactly at chance. The sufficiency result therefore separates into two claims: electrode bias alone suffices to \emph{discriminate} contact types, and Gaussian noise is required only so contact can be \emph{detected} at all, since a model without a noise floor cannot recognise a noisy vector as the no-contact state. The minimal model is one component for discrimination plus a noise floor for detection.

\begin{figure}[htbp]
\centering
\includegraphics[width=1\textwidth]{results/figures/ablation/per_component_severity_sweep.png}
\caption[Per-component severity sweep]{Per-component severity sweep. A CNN trained on the full noise model at 1$\times$ severity is evaluated with only one component active at varying severity. Enabling Gaussian noise recovers the no-contact class (a step of $\approx$20\,pp, independent of severity), while electrode bias is the only component whose severity materially degrades accuracy.}
\label{fig:severity-sweep}
\end{figure}

\subsection{Sensitivity to Component Ordering}
\label{sec:res-ablation-order}

Because the components are applied compositionally, their order can alter the resulting distribution. The effect is modest for the full four-component model (1.29\,pp between best and worst admissible ordering) but substantial within two-component subsets: Gaussian~$\to$~electrode bias achieves 76.53\%, reversed order only 54.98\%, a 21.6\,pp difference. This is a parametrisation artefact, not a physical effect: the Gaussian component is specified relative to signal amplitude, so applying it after the much larger bias inflates its magnitude accordingly (full derivation in Appendix~\ref{app:ablation}; implications discussed in Section~\ref{sec:disc-ablation}).

%% ─────────────────────────────────────────────────────────────────────────────
\section{Robustness Under Severity Scaling}
\label{sec:res-robustness}

Each training regime was evaluated across noise severities from 0$\times$ (clean) to 3$\times$ the calibrated level.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{results/figures/noise_type_severity_sweep.png}
\caption[Accuracy and macro-F1 against noise severity]{Accuracy and macro-F1 against noise severity for the three training regimes. The dotted line marks 1$\times$, the severity at which the fixed-noise model was trained. Fixed-noise training peaks at its training severity and degrades steadily beyond it. Randomised regimes are substantially flatter and overtake it at approximately 2.2$\times$.}
\label{fig:noise-type-sweep}
\end{figure}

\begin{table}[htbp]
\centering
\caption[Accuracy against noise severity multiplier]{Accuracy against noise severity multiplier by training regime.}
\label{tab:severity-sweep}
\small
\setlength{\tabcolsep}{6pt}
\begin{tabular}{@{}lccccccc@{}}
\toprule
\textbf{Regime} & \textbf{0$\times$} & \textbf{0.5$\times$} & \textbf{1$\times$} & \textbf{1.5$\times$} & \textbf{2$\times$} & \textbf{2.5$\times$} & \textbf{3$\times$} \\
\midrule
Fixed noisy         & 55.15 & 75.52 & \textbf{75.95} & 73.95 & 70.75 & 66.32 & 60.99 \\
Online augmentation & 41.44 & 67.60 & 68.91 & 68.96 & 68.67 & \textbf{68.19} & 67.55\\
Mixed               & \textbf{70.40} & 69.25 & 69.79 & 69.79 & 69.25 & 68.88 & 68.35 \\
\bottomrule
\end{tabular}
\end{table}

The regimes exhibit a clear trade-off between peak accuracy and breadth of robustness (Table~\ref{tab:severity-sweep}). Fixed-noise training is best at the severity it was trained on (75.95\% at 1$\times$) but degrades monotonically thereafter, losing 15.0\,pp by 3$\times$. The randomised regimes, trained across severities sampled from $[0.5, 2.0]$, are markedly flatter, with online augmentation varying by only 1.4\,pp between 1$\times$ and 3$\times$. The curves cross at approximately 2.2$\times$, beyond which randomisation is superior.

This is the behaviour domain randomisation predicts \parencite{tobin2017}: distributing training capacity across a range of conditions costs accuracy at any single condition but buys insensitivity across the range. Mixed training additionally retains clean-domain competence, 70.40\% at 0$\times$ against 41.44\% for online augmentation (implications for regime selection in Section~\ref{sec:disc-tradeoff}).

The 0$\times$ column is informative in itself: all three regimes lose accuracy on perfectly clean data, most severely online augmentation, largely the no-contact class (Section~\ref{sec:res-ablation-minimal}), since a model trained only on corrupted measurements has never observed a true zero vector.

%% ─────────────────────────────────────────────────────────────────────────────
\section{Supporting Analyses}
\label{sec:res-extended}

\subsection{Per-Class Vulnerability}
\label{sec:res-perclass}

\begin{figure}[htbp]
\centering
\includegraphics[width=1\textwidth]{results/figures/per_class_robustness.png}
\caption[Per-class accuracy against noise severity]{Per-class accuracy against noise severity for the noise-augmented CNN. Point contact is the weakest class throughout; the no-contact class is undetectable at 0$\times$ severity because it appears as an exact zero vector never encountered in training.}
\label{fig:per-class}
\end{figure}

Per-class behaviour (Figure~\ref{fig:per-class}) is not uniform, and the ordering is physically interpretable against the per-class signal magnitudes of Table~\ref{tab:noise-decomposition}. \emph{Point contact} is the weakest class at every severity (39\% at 0.5$\times$ falling to 29\% at 3$\times$), consistent with two compounding factors: the second-smallest voltage signature ($L_2 = 0.0058$) and, as established by the mesh convergence study (Section~\ref{sec:method-mesh}), being intrinsically sub-mesh-scale, the class whose simulation is least faithful. Its poor classification is therefore attributable partly to the model and partly to the fidelity limits of its representation.

\emph{Firm press} and \emph{distributed contact}, the two largest-amplitude classes, are the most reliably recovered. \emph{Light touch} improves slightly with severity (75.7\% to 90.9\%): counter-intuitive, but the same mechanism as the Gaussian result above, additional noise making the smallest-signal classes distinguishable from no-contact rather than absorbed into it. Accuracy for \emph{no contact} is 0\% at 0$\times$ and $\geq$97\% at every non-zero severity.

The safety implication is specific: detecting the onset of contact, the transition most relevant to grasp control, is reliable under the trained noise conditions, but the class carrying the highest local pressure (point contact) is the least reliable, confused with other contact classes rather than no-contact. A controller could therefore be expected to detect \emph{that} contact has occurred more reliably than to characterise \emph{what} it was.

\subsection{Predictive Confidence}
\label{sec:res-calibration}

The Bayesian tradition in \gls{eit} treats quantified uncertainty as integral to the solution \parencite{kaipio2005statistical}, yet ML-based classification reports bare point predictions. Table~\ref{tab:calibration} compares each model's mean predicted confidence against the accuracy it achieves.

\begin{table}[htbp]
\centering
\caption[Mean predicted confidence against actual accuracy]{Mean predicted confidence against actual accuracy.}
\label{tab:calibration}
\small
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Model $\to$ Data} & \textbf{Mean confidence} & \textbf{Accuracy} & \textbf{Gap} \\
\midrule
Clean CNN $\to$ clean      & 96.4\% & 93.8\% & $+2.6$ \\
Clean CNN $\to$ noisy      & 99.7\% & 19.7\% & $\mathbf{+80.0}$ \\
Augmented CNN $\to$ clean  & 55.4\% & 39.6\% & $+15.8$ \\
Augmented CNN $\to$ noisy  & 64.4\% & 68.8\% & $-4.4$ \\
\bottomrule
\end{tabular}
\end{table}

In its own domain, the clean-trained model is trustworthy (96.4\% confidence against 93.8\% accuracy), but outside it the correspondence collapses entirely (99.7\% confidence at 19.7\% accuracy, an 80-point gap), the most dangerous failure mode available to a safety-critical classifier since the softmax carries no warning that the model has left its domain. The noise-augmented model shows the opposite pattern on noisy data, confidence slightly \emph{understating} accuracy (64.4\% against 68.8\%). Overconfidence is therefore a symptom of \emph{domain shift} rather than an intrinsic architectural property, refining the observation of \textcite{guo2017calibration} that modern networks are overconfident by construction.

\subsection{Sensitivity to Modelling Assumptions}
\label{sec:res-sensitivity}

Because the noise parameters are nominal rather than measured (Section~\ref{sec:method-noise-summary}), each was swept independently with the others held at default (Table~\ref{tab:noise-sensitivity}).

\begin{table}[htbp]
\centering
\caption[CNN accuracy under independent noise parameter variation]{CNN accuracy (\%) as each noise parameter is varied independently, others held at default. Default settings in bold.}
\label{tab:noise-sensitivity}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{SNR (dB)}          & 60 & 50 & \textbf{40} & 30 & 20 \\
\quad Accuracy             & 69.3 & 69.3 & \textbf{69.4} & 69.3 & 69.3 \\
\midrule
\textbf{Contact imp.\ (\%)} & 5 & \textbf{10} & 15 & 20 & 25 \\
\quad Accuracy             & 69.2 & \textbf{69.4} & 69.5 & 69.4 & 69.1 \\
\midrule
\textbf{Bias $b_{\max}$}   & 0.005 & 0.01 & \textbf{0.02} & 0.04 & 0.08 \\
\quad Accuracy             & 68.6 & 69.9 & \textbf{69.4} & 64.9 & 35.0 \\
\midrule
\textbf{ADC (bits)}        & 8 & 10 & 12 & 14 & \textbf{16} \\
\quad Accuracy             & 30.1 & 69.3 & 69.7 & 69.4 & \textbf{69.4} \\
\bottomrule
\end{tabular}
\end{table}

The conclusions are insensitive to SNR and to contact impedance across their full plausible ranges (variation $<$0.5\,pp), reassuring since contact impedance is the parameter least well-constrained by the literature. Two parameters do matter: accuracy collapses to 35.0\% when electrode bias is quadrupled to $b_{\max} = 0.08$, and to 30.1\% at 8-bit ADC resolution. Both are interpretable: the former pushes the structured offset beyond the amplitude at which the model learned to discount it, while the latter raises the quantisation step until it is comparable to the touch signal itself, only 0.0040 for light touch. The engineering implications are discussed in Section~\ref{sec:disc-deployment}.

Hyperparameter sensitivity is comparatively modest, accuracy varying by 2.2--5.4\,pp across learning rate, dropout, weight decay and training severity range (Appendix~\ref{app:sensitivity}).

\subsection{Generalisation Across Noise Realisations}
\label{sec:res-memorisation}

Because train and test noise are drawn from the same parametric family, it must be confirmed that the model accommodates the noise \emph{distribution} rather than memorising the realisations in its training set. Evaluated on ten independently generated noisy test sets, each with entirely different electrode-bias, contact-impedance and Gaussian draws, accuracy is stable at 75.61\% $\pm$ 0.17\%, direct evidence of distributional accommodation rather than memorisation. Whether training noise is fixed once per sample or redrawn every epoch likewise makes no measurable difference (69.09\% against 69.50\% for online augmentation). What matters is distributional coverage, not realisation-level correspondence: a far less restrictive requirement than device-specific calibration. Full figures are in Appendix~\ref{app:memorisation}.
